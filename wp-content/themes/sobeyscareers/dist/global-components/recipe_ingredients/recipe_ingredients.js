(function($) {

  var $recipe_ingredients = $('[data-js-recipe_ingredients]');
  console.log('recipe_ingredients');
  if (! $recipe_ingredients.length) {
	return; // Return if component isn't on the page
  }

}(jQuery) );
