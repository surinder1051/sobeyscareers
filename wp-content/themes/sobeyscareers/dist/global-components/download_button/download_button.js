(function($) {

  var $download_button = $('[data-js-download_button]');
  console.log('download_button');
  if (! $download_button.length) {
	return; // Return if component isn't on the page
  }

}(jQuery) );
