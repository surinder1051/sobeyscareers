(function($) {

	var $related_recipies = $('[data-js-related_recipies]');
	console.log('related_recipies');
	if (! $related_recipies.length) {
		return; // Return if component isn't on the page
	}

}(jQuery) );
