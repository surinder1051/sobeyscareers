(function($) {

  var $recipe_nutrition_facts = $('[data-js-recipe_nutrition_facts]');
  console.log('recipe_nutrition_facts');
  if (! $recipe_nutrition_facts.length) {
	return; // Return if component isn't on the page
  }

}(jQuery) );
