(function($) {

	var $recipe_characteristics = $('[data-js-recipe_characteristics]');
	console.log('recipe_characteristics');
	if (! $recipe_characteristics.length) {
		return; // Return if component isn't on the page
	}

}(jQuery) );
