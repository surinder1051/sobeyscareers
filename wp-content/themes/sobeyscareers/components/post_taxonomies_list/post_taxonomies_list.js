(function($) {

  var $post_taxonomies_list = $('[data-js-post_taxonomies_list]');
  console.log('post_taxonomies_list');
  if (! $post_taxonomies_list.length) {
	return; // Return if component isn't on the page
  }

}(jQuery) );
