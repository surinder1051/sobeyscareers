(function($) {

  var $recipe_details = $('[data-js-recipe_details]');
  console.log('recipe_details');
  if (! $recipe_details.length) {
	return; // Return if component isn't on the page
  }

}(jQuery) );
